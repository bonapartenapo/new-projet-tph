export const sum = items => items.reduce((acc,item) => acc + item, 0)

/**
 * permet de conduire
 * 
 * @ereturn {((str: string, age: number) => Post)}
 */
async function fetchPosts(){

}
fetchPosts()

/**
 * @type {string[]}
 * 
 */
const b = []